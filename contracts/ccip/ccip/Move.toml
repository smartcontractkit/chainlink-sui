[package]
name = "ccip"
edition = "2024"

[dependencies]
mcms = { local = "../../mcms/mcms" }

[environments]
devnet = "a9149565"
# Sui public testnet chain identifier (matches mcms/Published.toml and Move.lock)
testnet = "4c78adac"
